# نظام طباعة وإدارة الشيكات

نظام احترافي لطباعة وإدارة الشيكات مبني بتقنية Next.js 15 مع دعم كامل للغتين العربية والإنجليزية.

## 🎯 نظرة عامة

هذا **تطبيق واجهة مستخدم فقط** مصمم كأساس مرئي لنظام طباعة شيكات احترافي. يتضمن:

- واجهة نظيفة واحترافية على مستوى المؤسسات
- دعم كامل للغة العربية (RTL) والإنجليزية (LTR)
- محرر قوالب بخاصية السحب والإفلات
- واجهة معالجة دفعات الشيكات
- جداول بيانات شاملة مع البحث والتصفية

## 🚀 البدء السريع

### المتطلبات الأساسية

- Node.js 18+ 
- npm أو yarn

### التثبيت

1. تثبيت الحزم المطلوبة:

```bash
npm install
```

2. تشغيل خادم التطوير:

```bash
npm run dev
```

3. افتح المتصفح على [http://localhost:3000](http://localhost:3000)

## 🌍 نظام اللغات المتعدد

### اللغة الافتراضية

- **العربية** هي اللغة الافتراضية للنظام
- يتم عرض الواجهة من اليمين لليسار (RTL) تلقائياً
- جميع النصوص والأزرار بالعربية

### التبديل بين اللغات

يمكنك التبديل بين العربية والإنجليزية من خلال:

1. النقر على زر اللغة في الشريط العلوي (أيقونة الكرة الأرضية)
2. سيتغير النظام بالكامل للغة المختارة
3. يتم حفظ اختيارك تلقائياً

### ما يتغير عند تبديل اللغة:

- ✅ اتجاه الواجهة (RTL/LTR)
- ✅ جميع النصوص والقوائم
- ✅ الأزرار والنماذج
- ✅ الجداول والبيانات
- ✅ الرسائل والإشعارات

## 📁 هيكل المشروع

```
check/
├── src/
│   ├── app/                         # جميع الصفحات
│   │   ├── page.tsx                # لوحة التحكم
│   │   ├── print-check/            # طباعة شيك واحد
│   │   ├── template-editor/        # محرر القوالب
│   │   ├── batch-printing/         # الطباعة الجماعية
│   │   └── printed-checks/         # الشيكات المطبوعة
│   ├── components/                  # مكونات قابلة لإعادة الاستخدام
│   ├── contexts/                    # إدارة اللغة
│   │   └── LanguageContext.tsx     # نظام اللغات
│   └── lib/
│       ├── mock-data.ts            # البيانات التجريبية
│       └── translations.ts         # ملفات الترجمة
```

## 🎨 الميزات والشاشات

### 1. لوحة التحكم (`/`)
- نظرة عامة على النظام مع مقاييس رئيسية
- آخر الأنشطة
- إجراءات سريعة
- إحصائيات مرئية

### 2. طباعة شيك (`/print-check`)
- اختيار الدولة والبنك
- اختيار القالب
- نموذج تفاصيل الشيك (التاريخ، المستفيد، المبلغ، إلخ)
- معاينة مباشرة للشيك مع دعم RTL/LTR
- تمثيل بصري لتخطيط الشيك

### 3. محرر القوالب (`/template-editor`)
- وضع الحقول بالسحب والإفلات باستخدام DND Kit
- محرر خصائص الحقول (الخط، الحجم، المحاذاة، الدوران)
- واجهة تصميم قائمة على اللوحة
- مكتبة حقول مرئية
- مساحة لتحميل القوالب

### 4. الطباعة الجماعية (`/batch-printing`)
- لوحة إعدادات الدفعة
- واجهة استيراد CSV/Excel
- إدخال الشيكات يدوياً
- ملخص الدفعة مع الإجماليات
- جدول شيكات قابل للتعديل
- وظيفة التصدير

### 5. الشيكات المطبوعة (`/printed-checks`)
- بحث وتصفية شاملة
- جدول بيانات مع ترقيم الصفحات
- مؤشرات الحالة
- دعم الإجراءات الجماعية
- خيارات التصدير وإعادة الطباعة
- لوحة إحصائيات

## 🛠 المجموعة التقنية

- **الإطار:** Next.js 15 (App Router)
- **التنسيق:** Tailwind CSS
- **السحب والإفلات:** DND Kit
- **الأيقونات:** Lucide React
- **اللغة:** TypeScript
- **إدارة اللغات:** React Context API
- **الخطوط العربية:** Cairo (للعناوين) + Tajawal (للنصوص)

## 🌐 كيفية عمل نظام الترجمة

### ملف الترجمات (`src/lib/translations.ts`)

يحتوي على جميع النصوص بالعربية والإنجليزية:

```typescript
export const translations = {
  ar: {
    nav: {
      dashboard: 'لوحة التحكم',
      printCheck: 'طباعة شيك',
      // ... المزيد
    }
  },
  en: {
    nav: {
      dashboard: 'Dashboard',
      printCheck: 'Print Check',
      // ... more
    }
  }
};
```

### استخدام الترجمات في المكونات

```typescript
import { useLanguage } from '@/contexts/LanguageContext';

function MyComponent() {
  const { t, language, direction } = useLanguage();
  
  return (
    <div>
      <h1>{t.nav.dashboard}</h1>
      {/* سيعرض "لوحة التحكم" بالعربية أو "Dashboard" بالإنجليزية */}
    </div>
  );
}
```

## 📝 البيانات التجريبية

جميع الشاشات تستخدم بيانات تجريبية من `src/lib/mock-data.ts`:

- دول مع إعدادات RTL/LTR
- بنوك حسب الدولة
- قوالب شيكات
- خيارات العملات
- شيكات مطبوعة نموذجية
- بيانات دفعات الشيكات

## 🎨 فلسفة التصميم

تتبع هذه الواجهة أفضل ممارسات البرمجيات المالية للمؤسسات:

- **الوضوح:** تسلسل هرمي وبنية مرئية واضحة
- **الدقة:** مناسب لإدخال البيانات المالية
- **القابلية للتنبؤ:** أنماط متسقة في جميع أنحاء النظام
- **سهولة الوصول:** HTML دلالي وتباين مناسب
- **الاستجابة:** تصميم يعطي الأولوية للهاتف المحمول

## 🔄 الخطوات التالية للتنفيذ الكامل

هذا **هيكل واجهة مرئية**. لجعله وظيفياً:

1. **تكامل الخادم:**
   - الاتصال بنقاط API
   - استبدال البيانات التجريبية بجلب بيانات حقيقية

2. **إدارة الحالة:**
   - إضافة Zustand/Redux للحالة العامة
   - تنفيذ التحقق من صحة النموذج

3. **منطق الأعمال:**
   - توليد أرقام الشيكات
   - تحويل المبلغ إلى كلمات
   - توليد PDF للطباعة
   - استمرارية القوالب

4. **المصادقة:**
   - نظام تسجيل دخول المستخدم
   - التحكم في الوصول حسب الدور
   - تسجيل المراجعة

## 📦 البناء للإنتاج

```bash
npm run build
npm start
```

## 🎨 التخصيص

### الألوان

عدّل `tailwind.config.ts` لتغيير نظام الألوان:

```typescript
colors: {
  primary: { /* ألوان علامتك التجارية */ },
  neutral: { /* درجات الرمادي */ }
}
```

### الخطوط

حدّث `src/app/layout.tsx` لاستخدام خطوط مخصصة.

### إضافة ترجمات جديدة

1. افتح `src/lib/translations.ts`
2. أضف النص الجديد لكل من `ar` و `en`
3. استخدم `t.yourNewKey` في مكوناتك

## 🆘 المساعدة والدعم

### حل المشاكل الشائعة

**المنفذ 3000 مستخدم؟**
```bash
npm run dev -- -p 3001
```

**مشاكل في التثبيت؟**
```bash
rm -rf node_modules package-lock.json
npm install
```

**أخطاء في البناء؟**
```bash
rm -rf .next
npm run dev
```

## 📄 الترخيص

هذا مشروع نموذج للواجهة. استخدمه بحرية في مشاريعك.

## 🎯 ملاحظات مهمة

- ⚠️ **واجهة المستخدم فقط** - بدون خادم، بدون منطق أعمال (بالتصميم)
- ✅ **جميع البيانات تجريبية** - لأغراض العرض التوضيحي
- ✅ **واجهة جاهزة للإنتاج** - كود نظيف واحترافي
- ✅ **سهل التوسيع** - جاهز لإضافة الميزات

## 🌟 الميزات الرئيسية للنظام ثنائي اللغة

### ✅ تبديل تلقائي للاتجاه
- العربية → RTL (من اليمين لليسار)
- الإنجليزية → LTR (من اليسار لليمين)

### ✅ ترجمة شاملة
- جميع النصوص مترجمة
- القوائم والأزرار
- الرسائل والإشعارات
- تسميات النماذج

### ✅ تجربة مستخدم سلسة
- لا حاجة لإعادة تحميل الصفحة
- حفظ تلقائي للغة المختارة
- انتقال سلس بين اللغات

---

**نظام طباعة الشيكات الخاص بك جاهز الآن!** 🚀

تم تلبية جميع المتطلبات مع كود نظيف واحترافي وقابل للصيانة. دعم كامل للعربية والإنجليزية مع العربية كلغة افتراضية.

