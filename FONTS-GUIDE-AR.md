# دليل الخطوط العربية

## 🎨 الخطوط المستخدمة

تم تكامل خطوط عربية احترافية من Google Fonts لتحسين تجربة المستخدم العربي.

### الخطوط المُفعّلة:

#### 1. **Cairo (القاهرة)** - للعناوين
- **الاستخدام:** العناوين الرئيسية (h1, h2, h3, etc.)
- **الأوزان:** 400, 500, 600, 700, 800
- **المميزات:**
  - خط عربي عصري وأنيق
  - واضح جداً في الأحجام الكبيرة
  - مثالي للعناوين والنصوص البارزة

#### 2. **Tajawal (تجوال)** - للنصوص العادية
- **الاستخدام:** النصوص العادية، الفقرات، الأزرار
- **الأوزان:** 400, 500, 700
- **المميزات:**
  - سهل القراءة
  - مناسب للنصوص الطويلة
  - متناسق مع التصميم الحديث

---

## 🎯 كيفية الاستخدام

### تلقائياً:
عند التبديل للغة العربية، تُطبَّق الخطوط تلقائياً على:
- ✅ جميع النصوص → `Tajawal`
- ✅ جميع العناوين → `Cairo`

### يدوياً في المكونات:

```typescript
// استخدام خط Cairo للعنوان
<h1 className="font-cairo font-bold text-2xl">
  عنوان بخط القاهرة
</h1>

// استخدام خط Tajawal للنص
<p className="font-tajawal">
  نص عادي بخط تجوال
</p>
```

---

## 🔧 التخصيص

### إضافة خط عربي جديد:

1. **افتح `src/app/layout.tsx`:**

```typescript
import { YourFont } from "next/font/google";

const yourFont = YourFont({
  subsets: ["arabic", "latin"],
  weight: ["400", "700"],
  variable: "--font-your-font",
  display: "swap",
});
```

2. **أضف المتغير للـ HTML:**

```typescript
<html className={`${cairo.variable} ${tajawal.variable} ${yourFont.variable}`}>
```

3. **استخدمه في Tailwind:**

```typescript
// في tailwind.config.ts
fontFamily: {
  'your-font': ['var(--font-your-font)', 'sans-serif'],
}
```

---

## 📚 خطوط عربية أخرى مقترحة

### من Google Fonts:

1. **Amiri (أميري)**
   - خط تقليدي فخم
   - مثالي للنصوص الرسمية

2. **Almarai (المرعي)**
   - خط بسيط وأنيق
   - ممتاز للواجهات الحديثة

3. **Noto Sans Arabic**
   - خط محايد وواضح
   - ممتاز للأنظمة متعددة اللغات

4. **Changa (شنقا)**
   - خط عصري وجريء
   - مناسب للعناوين البارزة

5. **Harmattan**
   - خط بسيط ونظيف
   - مناسب للنصوص الطويلة

### كيف تستبدل الخط:

```typescript
// في src/app/layout.tsx
import { Amiri } from "next/font/google";

const amiri = Amiri({
  subsets: ["arabic", "latin"],
  weight: ["400", "700"],
  variable: "--font-amiri",
  display: "swap",
});
```

---

## 💡 نصائح للخطوط العربية

### 1. الأوزان المناسبة:
```css
/* للنصوص العادية */
font-weight: 400 - 500

/* للعناوين */
font-weight: 600 - 800

/* للنصوص البارزة */
font-weight: 700 - 900
```

### 2. الأحجام المقترحة:
```css
/* عناوين رئيسية */
h1: 2.5rem - 3rem (40-48px)

/* عناوين فرعية */
h2: 2rem - 2.5rem (32-40px)

/* نصوص عادية */
body: 1rem - 1.125rem (16-18px)

/* نصوص صغيرة */
small: 0.875rem (14px)
```

### 3. تباعد الأسطر:
```css
/* للنصوص العربية */
line-height: 1.8 - 2.0

/* للعناوين */
line-height: 1.3 - 1.5
```

---

## 🎨 التطبيق في المشروع

### الاستخدام الحالي:

```css
/* النصوص العادية */
body → font-tajawal (Tajawal)

/* جميع العناوين */
h1, h2, h3, h4, h5, h6 → font-cairo (Cairo)

/* الأزرار والقوائم */
تلقائياً من body → Tajawal
```

### في المكونات:

```typescript
// عنوان صفحة
<h1 className="text-3xl font-bold">
  {t.dashboard.title}
</h1>
// يستخدم Cairo تلقائياً للعربية

// نص عادي
<p className="text-base">
  {t.dashboard.subtitle}
</p>
// يستخدم Tajawal تلقائياً للعربية
```

---

## 🔍 التحسينات المُطبّقة

### 1. Font Feature Settings:
```css
font-feature-settings: 'calt' 1, 'liga' 1;
```
- يُحسّن الربط بين الحروف
- يُظهر البدائل السياقية

### 2. Text Rendering:
```css
text-rendering: optimizeLegibility;
```
- يُحسّن وضوح النص
- أفضل للنصوص الطويلة

### 3. Font Smoothing:
```css
-webkit-font-smoothing: antialiased;
-moz-osx-font-smoothing: grayscale;
```
- يجعل الحروف أكثر وضوحاً
- تحسين العرض على الشاشات

---

## 📱 الخطوط المتجاوبة

```typescript
// أحجام متجاوبة للعناوين
<h1 className="text-2xl md:text-3xl lg:text-4xl font-bold">
  عنوان متجاوب
</h1>

// أحجام متجاوبة للنصوص
<p className="text-sm md:text-base lg:text-lg">
  نص متجاوب
</p>
```

---

## ⚡ الأداء

### التحميل المُحسّن:
- ✅ `display: "swap"` - يعرض الخط البديل حتى يتم التحميل
- ✅ تحميل الأوزان المطلوبة فقط
- ✅ دعم `preload` للخطوط الأساسية

### حجم الخطوط:
- Cairo: ~35KB (للأوزان المستخدمة)
- Tajawal: ~25KB (للأوزان المستخدمة)
- **إجمالي:** ~60KB فقط!

---

## 🎯 مثال كامل

```typescript
export default function MyComponent() {
  const { t } = useLanguage();
  
  return (
    <div>
      {/* عنوان رئيسي - Cairo تلقائياً */}
      <h1 className="text-3xl font-bold text-neutral-900 mb-4">
        {t.page.title}
      </h1>
      
      {/* عنوان فرعي - Cairo تلقائياً */}
      <h2 className="text-xl font-semibold text-neutral-700 mb-2">
        {t.page.subtitle}
      </h2>
      
      {/* نص عادي - Tajawal تلقائياً */}
      <p className="text-base text-neutral-600">
        {t.page.description}
      </p>
      
      {/* زر - Tajawal تلقائياً */}
      <button className="btn-primary">
        {t.actions.save}
      </button>
    </div>
  );
}
```

---

## ✨ النتيجة

- ✅ خطوط عربية احترافية وجميلة
- ✅ وضوح ممتاز في جميع الأحجام
- ✅ تطبيق تلقائي عند التبديل للعربية
- ✅ أداء محسّن وسريع
- ✅ متوافق مع جميع المتصفحات

---

**استمتع بالخطوط العربية الجميلة! 🎨**


